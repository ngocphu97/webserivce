import * as AuthSelectors from './auth.selector';
import * as LoginPageSelectors from './login-page.selector';
import * as ResetPasswordSelectors from './reset-password-page.selector';

export {
  AuthSelectors,
  LoginPageSelectors,
  ResetPasswordSelectors
};
